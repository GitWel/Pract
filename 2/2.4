#include "pch.h"
#include <iostream>
#include <cmath>
using namespace std;


 int main()
 {
	 float a = 4.6, x = -0.7, y, t;

	 if (a <= x) {
		 y = a + log(x + a);
	 }
	 else {
		 y = sqrt(sin(a*x));
	 }
	 
	 if (a == y) {
		 t = (y / (a - x)) + ((a + x) / (y*y));
	 }

	 else if(a < y) {
		 t = tan(a*x) + cos(2 * a*y);
	 }

	 else {
		 t = y/(a-x);
	 }
	 cout << "a="<< a << endl << "x="<< x << endl<< "y=" << y << endl <<"t="<< t << endl;

		system("pause");
		return 0;
 }
