#include "pch.h"
#include <iostream>
#include <cmath>
using namespace std;


int main()
{
	char comp = 'i';
	float a = 3.5, x = -0.7, y, t;

	if (a <= x) {
		y = a + log(x + a);
	}
	else {
		y = sqrt(abs(sin(a*x)));
	}

	if (a == y) {
		t = (y / (a - x)) + ((a + x) / (y*y));
	}

	else if (a < y) {
		t = tan(a*x) + cos(2 * a*y);
	}

	else {
		t = y / (a - x);
	}
	cout << comp << " = sqrt(-1)" << endl;
	cout << "a=" << a << endl << "x=" << x << endl << "y=" << y <<comp<< endl << "t=" << t <<comp<< endl;

	system("pause");
	return 0;
}
